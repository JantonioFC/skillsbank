# Disabled - this is a backup/mirror repo, not the upstream.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled in the backup repo"
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # Never runs
    steps:
      - run: echo "disabled"
